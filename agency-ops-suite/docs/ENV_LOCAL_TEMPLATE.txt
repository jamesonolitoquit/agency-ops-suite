# Local Development Environment
# 
# This file contains sensitive credentials for local testing only.
# NEVER commit this file to git.
# It's already in .gitignore, but be extra careful.
#
# Usage:
# 1. Copy this template to apps/admin-dashboard/.env.local
# 2. Fill in values from your Supabase project
# 3. Restart development server: npm run dev --workspace apps/admin-dashboard

# ============================================================================
# DATABASE CONFIGURATION (Supabase)
# ============================================================================
# Get these from: https://supabase.com → Project Settings → API

# Your Supabase project URL
# Example: https://abcdefghijklmnop.supabase.co
NEXT_PUBLIC_SUPABASE_URL=https://YOUR_PROJECT_ID.supabase.co

# Your Supabase anon (public) key
# Safe to expose to browser (protected by RLS)
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# Service role key (NEVER expose in browser environment variables)
# This is server-only, only set in .env.local and .env.production
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# ============================================================================
# APPLICATION CONFIGURATION
# ============================================================================

# Use seed data for local testing (set to false after testing)
NEXT_PUBLIC_USE_SEED_DATA=true

# Allow bypassing auth for local development (set to false for production)
# When true, you can access dashboard without logging in
DEV_AUTH_BYPASS=false

# Allowed admin emails (comma-separated)
# You must log in with one of these emails to access the dashboard
ADMIN_EMAIL_ALLOWLIST=your-email@example.com

# ============================================================================
# LEAD INTAKE CONFIGURATION
# ============================================================================

# Shared secret for public site to authenticate lead submissions
# This must match the value in your public site's environment
# Generate a secure random string: openssl rand -hex 32
INTAKE_WEBHOOK_SECRET=your-local-secret-change-this-12345

# Rate limiting configuration (optional)
# How often to reset the rate limit counter (milliseconds)
INTAKE_RATE_LIMIT_WINDOW_MS=60000

# Maximum requests per IP per window
INTAKE_RATE_LIMIT_MAX_REQUESTS=30

# Idempotency key cache TTL (milliseconds)
INTAKE_IDEMPOTENCY_TTL_MS=600000

# ============================================================================
# DEBUG & LOGGING
# ============================================================================

# Enable debug logging
DEBUG=true

# Next.js analytics
NEXT_PUBLIC_ANALYTICS_DISABLED=true

# ============================================================================
# OPTIONAL: FOR LIVE INTAKE INTEGRATION TESTS
# ============================================================================
# Only set these if running: npm run test:intake-live
# These should match your actual Supabase credentials above

SMOKE_ENABLE_LIVE_INTAKE=false
SMOKE_INTAKE_SECRET=your-local-secret-change-this-12345
SMOKE_SUPABASE_URL=https://YOUR_PROJECT_ID.supabase.co
SMOKE_SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
