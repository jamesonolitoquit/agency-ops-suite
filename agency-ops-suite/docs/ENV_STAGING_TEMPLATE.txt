# Staging Environment Configuration
#
# Use this template for the staging deployment environment.
# It is safe to commit only if all secret values remain placeholders.
# For a real staging project, copy these values into your deployment platform's secret manager.

# ============================================================================
# DATABASE CONFIGURATION (Supabase - STAGING)
# ============================================================================
# Point these at your staging Supabase project.

NEXT_PUBLIC_SUPABASE_URL=https://YOUR_STAGING_PROJECT.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-staging-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-staging-service-role-key

# ============================================================================
# APPLICATION CONFIGURATION
# ============================================================================

NEXT_PUBLIC_USE_SEED_DATA=false
DEV_AUTH_BYPASS=false
ADMIN_EMAIL_ALLOWLIST=staging-ops@youragency.com

# ============================================================================
# LEAD INTAKE CONFIGURATION
# ============================================================================

INTAKE_WEBHOOK_SECRET=staging-secret-change-me
INTAKE_RATE_LIMIT_WINDOW_MS=60000
INTAKE_RATE_LIMIT_MAX_REQUESTS=30
INTAKE_IDEMPOTENCY_TTL_MS=600000

# ============================================================================
# STAGING SMOKE TESTS
# ============================================================================

STAGING_ENABLE_LIVE_INTAKE=false
STAGING_INTAKE_SECRET=staging-secret-change-me
STAGING_SUPABASE_URL=https://YOUR_STAGING_PROJECT.supabase.co
STAGING_SUPABASE_SERVICE_ROLE_KEY=your-staging-service-role-key
STAGING_BASE_URL=https://your-staging-deployment.example.com
