# Generated artifacts that should stay out of source control.
dev.log
dev-server.log
logs/
backups/
test-results/
test-*.js
.vercel
.env*.local

# Keep CI workflow tracked at repository root
# (app-level .gitignore should not block .github at repo root)
