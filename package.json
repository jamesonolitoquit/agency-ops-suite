{
  "name": "agency-ops-suite",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev:dashboard": "npm run dev --workspace apps/admin-dashboard",
    "build:dashboard": "npm run build --workspace apps/admin-dashboard",
    "dev:cli": "npm run dev --workspace apps/provisioning-cli",
    "build:cli": "npm run build --workspace apps/provisioning-cli"
  },
  "overrides": {
    "postcss": "^8.5.12",
    "next": {
      "postcss": "^8.5.12"
    }
  }
}
